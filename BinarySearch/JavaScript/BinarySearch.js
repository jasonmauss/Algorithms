const binarySearch = (inputValuesArray, valueToFind) => {

    let left = 0;
    let right = inputValuesArray.length - 1;

    while(left <= right) {
        let middle = Math.floor((left + right) / 2);
    }
    


};

// Unit Tests

